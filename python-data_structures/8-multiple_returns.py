#!/usr/bin/python3
def multiple_returns(sentence):
    tup = ()
    if not (sentence == ""):
        tup = (len(sentence), sentence[0])
        t = ""
        t = None
        print(t)
        return tup
    sentence = None
    tup = (0, sentence)
    return tup
    
